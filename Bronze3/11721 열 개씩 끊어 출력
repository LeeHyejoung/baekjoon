#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main(void)
{
    char word[10000];
    int i;
    
    scanf("%s", word);

    for (i = 0; word[i] != '\0'; i++) {
        printf("%c", word[i]);
        if (i % 10 == 9) {
            printf("\n");
        }
    }
    
    return 0;
}
