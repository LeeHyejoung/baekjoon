#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main(void)
{
	int a, b;
	int i = 0;

	while (i != EOF) {
		i = scanf("%d %d", &a, &b);
		if (i == EOF)
			return;
		printf("%d\n", a + b);
	}

	return 0;
}
