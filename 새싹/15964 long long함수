#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

long long func(long long a, long long b) {
	return (a + b) * (a - b);
}

int main(void)
{
	long long a, b;
	long long result;

	scanf("%lld %lld", &a, &b);
	result = func(a, b);
	printf("%lld\n", result);

	return 0;
}
